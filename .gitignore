/*
!/.gitignore
!/README.md
!/bin/
!/meta/
!/app/
!/sources.template.yml
!/start
!/docs/

# python bytecode (app/ runs stdlib python; keep the repo clean)
__pycache__/

# local migration working area (machine-local; already covered by /* above,
# listed explicitly so nobody whitelists it by accident)
/migration/
